1. 두께가 있는 Wire을 롤에 감는데 길이를 입력하면 회전량을 계산하는 함수를 작성한다. \
2. 회전량을 입력하는 Wire길이를 알수 있는 함수를 작성한다. 
3. 회전량은 degree 단위를 사용한다. 
4. Wire의 두께는 일정하다고 가정한다.
5. Wire의 두께는 mm 단위를 사용한다.
6. Wire의 길이는 m 단위를 사용한다.
7. 롤에 와이어가 모든 감긴 상태를 와이어 길이를 0으로 가정한다. 
8. 모든 와이어가 감긴 상태에서 롤에 회전하여 와이어를 풀면 회전량은 양수이다.
9. C++ 언어로 작성한다.
10. CMakeLists.txt 파일도 작성한다.
11. 롤에 와이어를 감을때 와이어가 겹치지 않는다고 가정한다.
12. 롤의 외경은 와이어가 모두 감긴상태이다. 
12. 롤의 내경은 와이어가 하나도 감기지 않은 상태이다.
13. 롤의 내경 반지름은 mm 단위를 사용한다.
14. 와이어가 롤에 감길때 회전량에 비례해서 반지름이 증가한다.
15. 롤의 내경 반지름 값을 입력할수 있는 함수를 작성한다.
16. 롤의 내경 반지름은 mm 단위를 사용한다.

[추가요구사항]
1. 위에서 요구사항으로 만든 클래스(RollWireCalculator)를 이용해서 RollWireMover라는 클래스를 작성한다.
2. RollWireMover는 Wire를 올리고 내릴때 가속시간, 정속속도, 감속시간, 현재이동할 길이를 입력받는다. 그러나 가속시간, 정속속도, 감속속도는 멤버변수로 만들어서 설정할수 있도록 한다.
3. RollWireMover는 Wire를 올리고 내릴때는 2가지 함수를 제공한다. 상태위치로 이동하고 절대위치로 이동하는 함수이다. 
4. 상태위치로 이동하는 함수는 현재 위치에서 상대적으로 이동할 길이를 입력받는다.음수면 올리고 양수면 내린다. 
5. 절대위치로 이동하는 함수는 절대 위치를 입력받는다. 0이면 완전히 올린 상태이다.
6. Wire가 올리고 내릴때는 S자 형태와 Trapezoid 형태의 속도 프로파일을 지원한다. 설정가능하게
7. 올리고 내릴는거는 Roll에 달린 모터를 제어하는 것이다.
8. 올리고 내릴때 Roll의 회전량을 1ms마다 계산해서 배열에 저장한다. 
9. Roll에 달린 모터는 가상 Motor 클래스로 작성한다.
10. 저장된 회전량 배열을 Motor 클래스에 전달해서 Motor클래스가 회전량 배열을 이용해서 모터를 제어하도록 한다.
11. 가상 Motor 클래스는 모터의 현재 위치를 알수 있는 함수를 제공한다.
12. 가상 Motor 클래스는 모터를 제어하는 함수를 제공한다.
13. Wire의 최대길이는 5m이고, 움직이는 정속구간 최대속도는 1m/s이다. 정속구간 최소속도는 0.01m/s이다.
14. Wire의 최대길이는 RollWireMover 클래스에서 설정할수 있도록 한다.
15. Wire의 최대길이를 초과해서 올리거나 내리면 안된다.
16. Wire를 올리고 내릴때 현재 위치를 알수 있는 함수를 제공한다.
17. Wire를 올리고 내릴때 멈출수 있는 함수를 제공한다.
18. Wire를 올리고 내릴때 현재 상태를 알수 있는 함수를 제공한다. (정지, 가속중, 감속중, 정속중)
19. Wire를 올리고 내릴때 이동이 완료되면 알려주는 콜백함수를 등록할수 있도록 한다.
20. Wire를 정속으로 내리고 올리고 하기때문에 Roll 반지름인 실시간으로 변하기 때문에 회전량은 정속구간이 없다.   
20. C++ 언어로 작성한다.
21. CMakeLists.txt 파일도 작성한다.
  




